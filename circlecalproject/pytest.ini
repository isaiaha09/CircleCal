[pytest]
testpaths = tests
; Set Django settings module for pytest-django (uppercase key avoids pytest warning)
DJANGO_SETTINGS_MODULE = circlecalproject.settings
; Only collect files starting with 'test_' to avoid stray '*_test.py' scripts
python_files = test_*.py
